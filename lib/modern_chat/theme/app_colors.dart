import 'package:flutter/material.dart';

/// This class contains all the colors used in the Modern Chat UI Kit
/// Using a separate class for colors makes it easier to maintain and update the theme
class AppColors {
  // Primary colors
  static const Color primary = Color(0xFF246BFD);
  static const Color secondary = Color(0xFF1E2630);
  
  // Background colors
  static const Color background = Color(0xFFFFFFFF);
  static const Color secondaryBackground = Color(0xFFF8F9FA);
  
  // Text colors
  static const Color textPrimary = Color(0xFF1E2630);
  static const Color textSecondary = Color(0xFF78828A);
  
  // Message bubble colors
  static const Color sentMessage = Color(0xFF246BFD);
  static const Color receivedMessage = Color(0xFFF2F4F5);
  
  // Status colors
  static const Color online = Color(0xFF4CE417);
  static const Color offline = Color(0xFF78828A);
} 